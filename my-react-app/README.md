# SQB web app (Vite + React)

The Vite/React SPA served at smle-question-bank.com (Vercel project `medquiz-react`, built from `origin/main`). See the repo-root `README.md` for layout and how to run it, `docs/DESIGN_MAP.md` for the page and stylesheet map, and `docs/README.md` for every other doc.
